{
  "name": "saucepan-sdk",
  "version": "0.5.0",
  "description": "Typed Node.js client for the Saucepan central-store CLI",
  "license": "MIT",
  "type": "module",
  "engines": { "node": ">=20" },
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && tsc -p tsconfig.test.json && node --test test/client.test.mjs",
    "prepack": "npm run build"
  },
  "devDependencies": { "@types/node": "20.19.43", "typescript": "5.9.3" }
}
