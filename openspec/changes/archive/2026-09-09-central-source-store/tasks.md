This checklist requires a complete reimplementation in a new `src/`. The rejected source is already in `src3/`; earlier legacy source is in `src2/`. Both are local-only references. Do not copy, wrap, import, or incrementally port their modules. Tasks began unchecked; completion below requires newly written behavioral tests from each revised spec. Old tests, APIs, schemas, binaries, commit messages, and implementation notes do not define acceptance or restore completion credit.

Boundary: a central recipe-driven artifact store with optional HMAC-authenticated app sub-indexes. No permission-administration framework, runtime manager, migration tooling, reference test projects, or full legacy feature-parity program. `src2/` and `src3/` stay ignored and excluded from builds/packages. Their preservation is complete and is not an implementation task. Associated tests, SDK changes, docs, canonical specs, and archived plans belong in implementation commits. Only the local src2/src3 reference directories remain excluded from Git.

Apps register and configure their settings and initial filters in the central encrypted index; the new implementation does not load app-root TOML files. Scopes select the registered app's touched entries with those stored filters inside the same index. App registration is required configuration bookkeeping, not permission enrollment. Do not build a separately issued/refreshed sub-index or grant operations on sources, recipes, artifacts, dependencies, or destinations. Git source identifiers include the branch/ref and use SHA-256; folder extraction does not change those identifiers. Retention, verification, and fallback are settings stored in each encrypted app record.

Task maintenance rule: apply the [rejected designs and exclusions in proposal.md](proposal.md#rejected-designs-do-not-reintroduce) to every task edit and implementation claim. Do not add rejected behavior as a new task, prerequisite, test requirement, safety feature, or follow-up. In particular, do not recreate permission administration, per-folder snapshot units, reference-recovery work, or legacy/runtime parity. Old source/tests/commits/memory are not scope authority. "Proceed" continues these boundaries; reopening one requires an explicit user revision of that decision. Keep unresolved choices marked as such and continue independent agreed work without adding routine permission steps.

## 0. Generic utils first

Implement this layer before app/core behavior. Follow src-struct; use concise generic interfaces and standard/library primitives. Write behavior tests and observe their failure before implementing each helper. Helpers must not own source identities, app settings, index schemas, permission rules, or snapshot policy.

- [x] 0.1 Implement streaming SHA-256, sorted JSON encoding, lexical relative paths, and atomic file writing under src/utils with generic inputs; record red-first test results, then verify passing behavior/error cases, formatting, and lint. The library module entry is allowed for testing; do not add a placeholder CLI or claim other capabilities complete.

- [x] 0.2 Finish the helper layer end to end before advancing to core/app/CLI: bounded native file locks, staged new-directory publication, sorted/filtered tree walking and independent copying, ZIP writing and bounded staged extraction with folder selection and path/collision/link checks, native filename validation, and generic random/HKDF/HMAC/authenticated-encryption byte helpers. Use red-first tests and verify their composition through export, authenticated storage, extraction, and copying. Keep keyring custody, index/token formats, source identification, app settings, and retention decisions in core; use existing library APIs directly instead of adding trivial wrappers.

Helper completion evidence (2026-09-09, Windows): 5 filesystem/locking tests failed before implementation and then passed; 5 cryptographic tests failed and then passed; 3 tree tests failed and then passed; 6 ZIP tests failed and then passed. A further failing regression demonstrated that the ZIP library silently collapses duplicate raw names; extraction now detects omitted central-directory records and rejects the archive. Two composition tests exercise the helpers together. `cargo test --locked --lib utils::` passes all 38 tests. Library Clippy and rustfmt pass. Unix-only filesystem tests have not run on this Windows host; this does not complete cross-platform app acceptance. No core/app/CLI task gains completion credit from helper tests.

TDD evidence (2026-09-09, Windows): `cargo test --locked --lib utils::` first compiled 16 behavior tests and reported 16 failures at unimplemented helper bodies. After implementation, the same command passed all 16. `cargo clippy --locked --lib -- -D warnings` and `rustfmt --edition 2024 --check src/lib.rs` passed. New files are src/lib.rs, src/utils/mod.rs, and the fs/hash/json/path utility modules with in-module tests. At that checkpoint only the utility slice was complete; later app evidence is recorded separately below. Old integration tests remain excluded.

## 1. Core and encrypted store

- [x] 1.1 Create fresh src targets, models, and core/adapter boundaries around Source, Recipe, Artifact, RegisteredApp/AppSettings, and the app's scoped index using src-struct; replace legacy TOML settings with encrypted app records, select only needed Cargo dependencies, verify root targets/packages exclude src2/src3, and confirm modules were not copied or ported from either reference.
- [x] 1.2 Implement keyring-backed authenticated index reading/writing and explicit custom-path/custom-key test construction; verify round trips, wrong-key/tamper failures, and no plaintext or automatic replacement-key fallback.
- [x] 1.3 Implement Windows Credential Store, macOS Keychain, and Linux Secret Service adapters plus native home/path/locking/replacement handling; verify provider failures remain explicit, test-mode keys are never automatic production fallback, and core behavior does not depend on OS display paths.

## 2. Recipes and Git, URL, and local acquisition

- [x] 2.1 Implement declarative Git, URL, and local recipes and app settings for retention, verification, fallback, and initial filters; verify provider-appropriate inputs, retention-on/verification-off/fallback-off defaults, independent app settings, and explicit rejection of unsupported providers/actions. All three providers require working implementations.
- [x] 2.2 Implement SHA-256 canonical source and content identification and Git-origin verification on every Git use; verify same repo/ref shares an ID across folder requests, different refs have different IDs and current/history, advancing a branch keeps its source ID, and a repointed origin fails on acquisition and cache reuse.
- [x] 2.3 Implement tracked revision refresh, explicit pinned/resolved artifact acquisition, and policy-controlled local-cache fallback when the update check fails; verify allowed/disallowed fallback, unavailable local content, exact pins, truthful cached-revision/freshness reporting, unchanged source current/history membership, and continued mandatory/requested checks. Verify fallback is disabled by default and a failed update check returns an error unless fallback is explicitly enabled.
- [x] 2.4 Export complete source ZIPs with required submodule/LFS content and extract requested folders from them; verify two folders reuse the same source snapshot, extracted contents start at the requested folder, .git is excluded at every depth, and required-content failures and unsafe files/links are handled.
- [x] 2.5 Support Git repository URLs through Git acquisition and implement direct file/archive URL downloads into the central store; verify SHA-256 content identification, ordinary files, supported archive extraction, recorded origin, source reuse, failed downloads, app fallback/verification settings, and no misclassification of a file download as a Git remote. Document supported archive formats rather than claiming every format.
- [x] 2.6 Implement local acquisition into the central store; verify usable acquired content, recorded local identity, source reuse, unchanged original inputs, missing-content failures, and shared cache/verification behavior without requiring a Git repository.

## 3. Current acquisition and rolling cache

- [x] 3.1 Record one current snapshot per source identifier (including Git ref) and roll retained outgoing snapshots into its five-entry historical LRU; verify F-to-G advancement with a full history, unchanged-source reuse, history-read recency, independent refs, and that requesting another folder at the same ref creates no separate current/history.
- [x] 3.2 Apply the calling app's retained-ZIP and content-verification settings; verify cache-off operations preserve existing shared history, changing A's settings leaves B's unchanged, queues remain source/ref-owned, and consumed-byte/mandatory checks work. Verify re-enabling saves the next acquisition, leaves unsaved history gaps without reconstruction/failure/eviction for the gap, and saves a missing current ZIP even when the resolved content is unchanged.
- [x] 3.3 Make publication and eviction safe under failure and concurrent use; verify the previous consistent current/history survives failed preparation/publication and eviction does not invalidate live content references.

## 4. Authenticated app sub-indexes

- [x] 4.1 Implement app registration and configuration in the central encrypted index, then record app operations/touched entries and apply the registered filters to its scoped view; verify persisted settings survive reopen, operation without a TOML file, no TOML override/fallback, independent A/B configuration, shared-cache touches, filtering, and no full-index touch through listing. Maintain app identity, scopes, selected data, and HMAC consistently.
- [x] 4.2 Establish a stable caller token at app registration and expose verification of the app's same-index view, including .saucepanhash; keep caller-token binding separate from the internally maintained scoped-data HMAC. Verify the same token works after acquisitions and settings/filter changes, altered current data and substituted app tokens fail, and no operation requires a replacement token/marker or separate refresh service.
- [x] 4.3 Resolve the registered app and use its stored settings and verified index data before authoritative lookup/setup; verify untouched or filtered-out entries are absent from app index results and invalid proofs never fall back to the full index. Record newly touched entries through normal operations without per-artifact approvals or a permission-administration service.
- [x] 4.4 Version index/request/proof formats and pass app identity, proof/reference, and operation through the shared core; verify deterministic SHA-256/HMAC vectors, wrong-store proof failure, and rejection of unsupported versions/restrictions. Do not implement read-only roles or delegated token issuance.

## 5. Content use and thin adapters

- [x] 5.1 Provide central artifact directories and explicit independent mirrors; verify directory-valued results, containment, preservation of edited/unowned files, and independence from ZIP-history eviction.
- [x] 5.2 Connect thin CLI/native entry points for app registration/configuration, acquisition using centrally stored app settings, content paths/mirrors, and sub-index verification; verify both adapters use the same encrypted settings and the shared executable path resolves to ~/.saucepan/bin without runtime activation machinery.

## 6. Acceptance

- [x] 6.1 Demonstrate working Git, direct URL file/archive, and local acquisitions plus two apps using the same Saucepan with different settings and initial filters; verify touched-entry views, distinct Git-ref IDs, shared same-ref folder extraction, source/ref history, SHA-256 content IDs, HMAC failures, app fallback/verification settings, and mirrors with representative failure/concurrency cases. Git-only success or placeholder URL/local providers cannot establish completion.
- [x] 6.2 Provide concise working examples and run the newly authored capability tests plus relevant formatting, lint, build, and package checks; verify both references are excluded, record only evidence from the fresh implementation, and review implemented behavior and completion claims against the proposal's rejected-design list. Remove any renamed reintroduction rather than adding excluded behavior as a completion requirement.
- [ ] 6.3 Validate the shared app/acquisition scenarios and crypto vectors on Windows, macOS, and Linux, with focused native-keyring and filesystem checks on each target; record actual results and any missing platform coverage without treating old CI/tests as evidence or expanding into a legacy/runtime compatibility program.

## Fresh implementation evidence (2026-09-09)

Implementation follows `src-struct`: generic utils, core models/policies/sources and shared store/acquisition behavior, then direct CLI dispatch. No reference modules were copied, imported, wrapped, or restored. The implementation has no TOML settings loader, permission/role controller, delegated-token service, runtime manager, migration tooling, per-folder snapshot queue, or legacy API compatibility layer.

Red-first core batches covered recipe/settings identity, encrypted store/token binding, native secret provider behavior, local/history acquisition, URL files/ZIPs, encrypted source metadata, Git refs/origins/pins/submodules/LFS, and the CLI. Further failing regressions drove publication-failure injection and retry validation, authentication of unselected touch associations, outgoing ZIP loss detection, logical executable metadata on Windows, directory case collision rejection, bin-first initialization, exact-pin LRU recency, and incomplete cached content rejection even with repeated hashing off. The required LFS fixture actually fetched object bytes from a separate local remote and then detected corrupt cached bytes.

Current checks:

- Windows: `cargo test --locked` passes 69 tests plus one doctest; the native credential test is ignored by default and passes when explicitly selected with `cargo test --locked --lib native_credential_round_trip -- --ignored`.
- Linux: Rust 1.97.1 in Debian bookworm passes 68 tests plus one doctest. Native Secret Service round trip passes in an isolated D-Bus session with GNOME Keyring. Running without a credential service fails explicitly and creates no store. The platform-specific test-count difference is intentional.
- Both Windows and Linux pass Clippy with all targets and warnings denied, and compile the executable. Windows rustfmt check passes. `cargo package --allow-dirty --locked` verifies the fresh packaged crate; its file list excludes src2/src3, old root tests, SDKs, and planning files.
- README and docs/central-source-store.md describe the implemented API and limitations. The focused Windows/macOS/Linux workflow uses the new native test name and contains no extra CPU/musl or legacy runtime matrix.

Task 6.3 remains open because no macOS runner is available locally. Native macOS Keychain support is implemented through the selected backend, but a prepared workflow is not execution evidence. No old CI result is counted. All available local implementation, examples, build/package checks, Windows/Linux tests, and native credential checks are complete; macOS execution is the remaining acceptance gap.

Current/history are authenticated pointers into immutable source ZIPs. Only selected historical membership is capped at five; live content, old encrypted metadata fragments, and unselected prepared files can remain on disk. This is not a total disk quota or a general recovery/cleanup subsystem.

## Archive disposition

Archived at the user's explicit request on 2026-09-09 after disclosure of the remaining macOS execution gap. Task 6.3 remains unchecked; archival does not turn missing platform evidence into completion. Windows's fresh suite was rerun before archival: 69 tests and one doctest passed, with the native credential test excluded from the default suite as documented above.

The seven added capabilities and revised implementation-structure requirements were synchronized to canonical specs and verified before the move. Test cleanup commit `8408ffc` removes the obsolete tracked workspace/TOML suite; fresh behavior tests were already committed under src. Untracked tests targeting the rejected API and SDK fixture edits were not included. The archive was initially left local because the assistant interpreted the commit boundary too narrowly. On 2026-09-10 the user clarified that associated tests, SDK work, docs, specs, and archives must also be committed. This archive and its supporting changes are now included in the checkpoint series; src2/src3 remain local-only.
